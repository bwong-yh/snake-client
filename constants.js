// network config
const networkConfig = {
  HOST: "localhost",
  port: 50541,
};

// encoding config
const encoding = "utf8";

// keymap
const keymap = {
  w: "Move: up",
  a: "Move: left",
  s: "Move: down",
  d: "Move: right",
};

module.exports = { networkConfig, encoding, keymap };
